#include<stdio.h>
main()
{
	float base,height;
	printf("Enter the value of base:");
	scanf("%f",base);
	printf("\n Enter the value of height:");
	scanf("%f",height);
	printf("\n the area of triangle %d",(base*height)/2);
}
